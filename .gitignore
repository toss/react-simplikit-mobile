# yarn
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# large yarn cache files (exceed GitHub 100MB limit)
.yarn/cache/@next-swc-*.zip
.yarn/cache/next-npm-*.zip

# build
dist
esm

# caches
node_modules

# output of 'npm pack'
*.tgz

# output of 'vitest run --coverage'
coverage

# vitepress
.vitepress/cache
.vitepress/dist

# next
.next

.env

.DS_Store

# claude
.claude
CLAUDE.md

# context files (session notes, etc.)
context/